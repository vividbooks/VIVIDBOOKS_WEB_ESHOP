/** Jeden soubor ve `public/` — prohlížeč ho stáhne jednou a použije pro všechny tiskoviny (místo CSS drop-shadow na každé obálce). */
export const BOOK_COVER_FLOOR_SHADOW_SRC = `${import.meta.env.BASE_URL}images/book-cover-floor-shadow.svg`;
