/**
 * Ruční výjimky nad automatickými pravidly Webflow migrace.
 * Klíč = normalizovaná cesta (např. `/cs/stara-stranka`), hodnota = `/nova` nebo `https://…`
 */
export const WEBFLOW_LEGACY_MANUAL_OVERRIDES: Record<string, string> = {};
